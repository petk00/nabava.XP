# macOS
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db

# Editor
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
.vscode/settings.json

# Logs
*.log
npm-debug.log*
yarn-debug.log*

# Dependencies
node_modules/

# Environment (sadrže tajne ključeve — ne commitati)
# Obrazac je namjerno širok: .env.bak-uploads je prošao kroz .env.*.local i
# stigao do pusha s pravim Gemini ključem, koji je zaustavila tek GitHubova
# zaštita. Predlošci bez tajni izuzeti su izrijekom.
.env
.env.*
!.env.example

# Build output
dist/

# Test coverage i e2e artefakti (generirano, ne commitati)
coverage/
e2e/test-results/
e2e/playwright-report/

# AI asistent eval harness — sirovi rezultati (generirano, ne commitati)
server/eval-results/
db/backup/
backups/

# Sigurnosne kopije radnih datoteka (ručni artefakti — ne commitati)
*.bak
*.bak-*
